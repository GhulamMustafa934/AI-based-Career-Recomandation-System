# ─────────────────────────────────────────────────
# Career Path Recommender — Python Dependencies
# Install with: pip install -r requirements.txt
# ─────────────────────────────────────────────────

# Web framework
flask>=2.3.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Machine learning
scikit-learn>=1.3.0

# Data visualization (charts saved during training)
matplotlib>=3.7.0

# Resume file parsing
pdfplumber>=0.10.0      # PDF text extraction (primary)
pypdf>=3.0.0            # PDF text extraction (fallback)
python-docx>=1.1.0      # DOCX/DOC resume parsing
